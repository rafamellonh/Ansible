server {
    listen {{ nginx_port }};
    server_name {{ nginx_server_name }};

    root {{ nginx_doc_root }};
    index {{ nginx_index }};

    location / {
        try_files $uri $uri/ =404;
    }
}
