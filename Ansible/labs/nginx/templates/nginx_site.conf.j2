server {
    listen {{ item.listen }};
    server_name {{ item.server_name }};

    root {{ item.root }};
    index {{ item.index }};

    location / {
        try_files {{ item.location_try_files }};
    }

    {{ item.extra_parameters }}
}

