### Ordem de prioridade

* $ANSIBLE_CONFIG
* ./ansible.cfg
* /etc/ansible/ansible.cfg
* ~/.ansible.cfg


### Comandos

```
ansible-config list
ansible-config dump
ansible-config view
ansible-config init

```
